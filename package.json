{
  "name": "khoaluan3-backend",
  "version": "1.0.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "cd backend && npm install && npm run build",
    "start": "cd backend && npx prisma migrate deploy && node dist/index.js"
  }
}
